# Changesets

Versioning for `@calenderjs/*` packages. Create one before a feature release:

```bash
pnpm changeset
```

Qingniao consumes these during `pnpm release`. After a version bump, an empty pending list is expected until the next change.
