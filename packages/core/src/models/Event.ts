/**
 * Event 核心接口
 * 
 * 注意：Event 和 EventMetadata 已移至 @calenderjs/event-model 包
 * 此文件仅作为向后兼容的重新导出
 * 
 * @deprecated 请直接从 @calenderjs/event-model 导入
 * @see {@link @calenderjs/event-model}
 */

// 从 event-model 包重新导出，保持向后兼容
export type { Event, EventMetadata } from '@calenderjs/event-model';
